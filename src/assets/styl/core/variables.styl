// Colors
$colorPrimary = steelblue

//Fonts
$reserveFonts = sans-serif
$fontPrimary = 'OpenSans', $reserveFonts

//Colors
$mainColor = #000000

// Backgrounds
$mainBg = #FFFFFF

// Transition
$mainTransition = .3s

// Sizes
$headerHeight = 5rem
$footerHeight = 5rem
$maxWidth = 144rem
$maxInnerWidth = 106rem

// Media query
$mobileWidth = 500px
$tabletWidth = 768px
$desktopWidth = 1200px
